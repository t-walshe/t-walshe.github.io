This site uses Jekyll to build the website from the pages defined in the repo:

The following steps can be used to build the site locally:
1. `docker compose build`
2. `docker compose up`
3. Access `http://localhost:4000`